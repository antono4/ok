interface:
  display_name: "Ok"
  short_description: "Repo-specific patterns and workflows for ok"
  default_prompt: "Use the ok repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true